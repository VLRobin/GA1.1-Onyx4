In the subtract notebook a function is created which takes a and b as input. It prints what a and b are and the answer to the subtraction a-b. An extension function was added to determine the distance from zero of the substraction result.

Multiply(a, b): The function works by adding two variables and multiplying them. It might break if you add a non-number. An example: a=4 and b=5 gives the result 20.

Add-function: the function adds the two input variables.

Square function: the function calculates the square of the input variable. It calls the multiply function.